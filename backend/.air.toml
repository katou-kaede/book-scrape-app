root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
  # Windows用のビルドコマンド（cmd/api/main.go を指定）
  cmd = "go build -o ./tmp/main.exe ./cmd/api/main.go"
  # 実行するバイナリの場所
  bin = ".\\tmp\\main.exe"
  # 監視する拡張子
  include_ext = ["go", "tpl", "tmpl", "html", "toml"]
  # 無視するディレクトリ
  exclude_dir = ["assets", "tmp", "vendor", "frontend"]
  # ファイル変更からビルド開始までの待ち時間（ミリ秒）
  delay = 1000
  # ビルドエラー時に古いバイナリを動かし続けるか
  stop_on_error = true
  # プロセスを確実に終了させるための設定
  send_interrupt = true
  kill_delay = 500 # 終了を待つ時間（ミリ秒）

[log]
  time = true

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"

[screen]
  clear_on_rebuild = true